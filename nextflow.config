nextflow.enable.dsl=2
conda.enabled = true

params {
  // Reference genome
  species        = 'monodelphis_domestica' // Organism (genus_species)
  build          = 'ASM229v1'              // Assembly name
  ref_dir        = 'ref'                   // Reference genome base directory

  // FASTQ input
  fastq_mode     = 'local'                 // 'local' or 'download'
  fastq_dir      = 'data/fastq/test'       // FASTQ file location
  fastq_manifest = 'manifests/U01_Lein-Opossum.tsv'
  download_method = 'wget'                 // 'wget' (default) or 'aspera' (fast, needs open port 33001)
  download_only  = false                   // Set to true to stop after FASTQ download

  // Output
  out_dir        = 'results'

  // Alignment
  threads        = 8
  read_length    = 100
  star_index     = null                    // Set to path to skip STAR_INDEX
  bc_inclist     = 'ref/10x_v3_bc/3M-february-2018.txt'

  // Intergenic analysis
  min_mapq       = 30                      // Minimum mapping quality for BAM filtering
  bin_width      = 500                     // Histogram bin width (bp)
  min_distance   = -10000                  // Minimum signed distance (bp)
  max_distance   = 10000                   // Maximum signed distance (bp)
}

process {
  conda = "$HOME/.conda/envs/rnaseq"
  beforeScript = 'source /u/local/Modules/default/init/modules.sh && module load anaconda3 aspera-connect/3.10.1'
  withName:STAR_INDEX {
    cpus      = params.threads
    memory    = '48 GB'
    beforeScript = 'source /u/local/Modules/default/init/modules.sh && module load anaconda3 aspera-connect/3.10.1 star/2.7.10a'
  }
  withName:STARSOLO_ALIGN {
    cpus   = params.threads
    memory = '24 GB'
    beforeScript = 'source /u/local/Modules/default/init/modules.sh && module load anaconda3 aspera-connect/3.10.1 star/2.7.10a'
  }
  withName:CALC_DISTANCES {
    memory = '64 GB'
  }
}

profiles {
  // Local cluster
  standard {
    process.executor = 'local'
  }
  // UGE cluster
  uge {
    includeConfig 'config/uge.config'
  }
}